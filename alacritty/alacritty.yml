import:
  - ~/.config/alacritty/colors.yml
  - ~/.config/alacritty/fonts.yml

window:
  padding:
    x: 10
    y: 10

scrolling:
  history: 10000
  multiplier: 3

cursor:
  style:
    shape: Underline
    blinking: On

shell:
  program: /usr/bin/zsh

live_config_reload: true
